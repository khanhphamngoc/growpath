# Local dev defaults — safe to commit (Docker connection strings, no secrets).
# Secrets (ANTHROPIC_API_KEY, NEXTAUTH_SECRET, GOOGLE_*) go in .env.local only.
DATABASE_URL=postgresql://growpath:growpath@localhost:5432/growpath
REDIS_URL=redis://localhost:6379
